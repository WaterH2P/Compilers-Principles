package logic;

import symbol.Token;

import java.util.ArrayList;

public class Main {
    private static String input = "private boolean isKeyWord(String word){\n" +
            "        String sb = \\\"hello sb!\"" +
            "        if( defaultValue_KeyWord.contains(word) )\n" +
            "            return true;\n" +
            "        return false;\n" +
            "    }";
    private static ArrayList<Token> tokens = new ArrayList<>();
    private static JudgeClass jc = new JudgeClass();
    private static HandleString hs = new HandleString();

    public static void main(String[] args){
        String[] rowContent = hs.divideString(input);
        for (int i = 0; i < rowContent.length; i++) {
            System.out.println(rowContent[i]);
        }

//
//
//        for (int i = 0; i < tokens.size(); i++) {
//            System.out.println(tokens.get(i).showContent());
//        }
    }
}
